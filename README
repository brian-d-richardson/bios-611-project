bios-611-projectBrian RichardsonFall 2022I am interested in analyzing data on stages and participants in the Tour de France from 1903 to 2021.Broadly, I plan to investigate the following four things:1) how the design of the race has changed over time,2) how rider performance has changed over time,3) if rider performance changed during and after the period of Lance Armtstron's illegitimate wins,4) how riders' performance change over their careers.######################################################################################################INSTRUCTIONS######################################################################################################1) Download Docker2) Set your directory to "bios-611-project-main"3) Build a Docker container using the following command. The file ".password" contains the password   you will use to log into RStudio. By default, this is "my_pwd", but can be edited as you prefer.	docker build . --build-arg linux_user_pwd="$(cat .password)" -t bios-611-proj4) Open the Docker container using the following command:	docker run -v "$(pwd)":/home/rstudio/bios-611-proj-ws -p 8787:8787 -e PASSWORD="$(cat .password)" -it bios-611-proj5) Open RStudio by entering "http://localhost:8787" in your browser search bar.6) For users that only want to view the final report, open the Rstudio terminal and run the following two commands:	cd bios-611-proj-ws	make tdf-report.html7) Users that want to interact with the code can do so in Rstudio within the Docker container######################################################################################################THIS PROJECT INCLUDES######################################################################################################1) 2 derived data sets:		derived-data/TDF_Stages_History_Clean.csv	derived-data/TDF_Riders_History_Clean.csv2) 10 figures, which can be accessed:	(a) as separate pngs in "bios-611-project/figures"		(i) locations_plot		(ii) riders_per_year_plot		(iii) riding_time_over_time_plot		(iv) stages_per_year_plot		(v) team_wins_plot		(vi) rider_wins_plot		(vii) avg_speed_over_time_plot		(viii) avg_speed_over_time_plot_dope		(ix) distance_over_time_plot		(x) rel_ranks_plot	(b) as one pdf document: "bios-611-project/Rplots.pdf"	(c) as part of the html report in 3).3) A final report in the format of and html document.